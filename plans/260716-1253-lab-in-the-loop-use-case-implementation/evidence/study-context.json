{
  "task": "Build single-host resumable multimodal ingestion with content-hash deduplication, bounded local extraction, durable leased work, authenticated MCP controls, progress/chunk reads, and locality-governed evidence integration.",
  "mode": "auto",
  "acceptanceCriteria": [
    "SQLite/WAL ingestion state durably records canvas-scoped assets, jobs, deterministic units, chunks, attempts, cancellation, and lease generations so interrupted work resumes without rerunning completed units or accepting stale completion.",
    "Enqueue is idempotent by immutable content SHA-256 and extractor version: unchanged content reuses the existing job/cache while a version change creates only new derived work without changing the raw asset identity.",
    "Text, PDF, image-metadata, and table extraction is deterministic and bounded; malformed, encrypted, oversized, or unsupported inputs fail with typed sanitized states and never fabricate content or place raw bytes in model context.",
    "The standalone bounded worker supports retry, backoff, cancellation, lease renewal, graceful shutdown, poison-unit terminal failure, and restart recovery with durable progress and failure reasons.",
    "MCP ingestion tools expose enqueue, status, retry, cancel, and paginated chunk reads with centralized reader/trusted-service/operator authorization, loopback defaults, authenticated HTTP calls, canvas scope, and metadata-only denial audit.",
    "Status and chunk responses are bounded, pure reads that expose trusted top-level data classification and provenance without local paths, credentials, capability URLs, raw bytes, or hidden extraction/mutation side effects.",
    "Lab-agent permits only approved ingestion status/chunk reads, captures substantive bounded chunks as untrusted evidence, rejects mutation tools and arbitrary namespace suffixes, enforces inbound tool-result bounds, and applies Phase 5 locality before every subsequent provider call.",
    "Canvus-MCP and lab-agent test, lint, type-check, parity, build, crash/restart, deduplication, authorization, cancellation, bounded-output, and no-write-after-denial regressions pass without live credentials or fake bypasses.",
    "Architecture, operations, standards, roadmap, canonical specification, changelog, configuration, retention/capacity limits, extractor dependency boundaries, and the external-queue migration trigger accurately describe the implemented Phase 6 contract and remaining external gates."
  ],
  "touchpoints": [
    "apps/canvus-mcp/canvus_mcp/ingestion_store.py",
    "apps/canvus-mcp/canvus_mcp/ingestion_pipeline.py",
    "apps/canvus-mcp/canvus_mcp/extractors.py",
    "apps/canvus-mcp/canvus_mcp/ingestion_worker.py",
    "apps/canvus-mcp/canvus_mcp/access_control.py",
    "apps/canvus-mcp/canvus_mcp/tools/ingestion.py",
    "apps/canvus-mcp/canvus_mcp/server.py",
    "apps/canvus-mcp/canvus_mcp/config.py",
    "apps/canvus-mcp/canvus_mcp/downloads.py",
    "apps/lab-agent/lab_agent/tool_bridge.py",
    "apps/lab-agent/lab_agent/mcp_client.py",
    "apps/lab-agent/lab_agent/config.py",
    "apps/lab-agent/lab_agent/cli.py",
    "apps/lab-agent/tests/",
    "apps/canvus-mcp/tests/",
    "docs/"
  ],
  "blastRadius": [
    "download metadata and local cache persistence",
    "SQLite migration, lease, retry, and cancellation semantics",
    "MCP HTTP and stdio authentication/authorization",
    "existing canvas mutation tool compatibility",
    "ingestion tool schemas and progress visibility",
    "model-facing read-tool allowlist and namespace handling",
    "evidence provenance and provider locality authorization",
    "single-host disk, retention, and worker capacity"
  ],
  "contracts": [
    "Existing download tools keep their additive path, MIME, size, SHA-256, filename, canvas, and widget metadata contracts and do not implicitly enqueue extraction.",
    "Raw files remain in ignored local storage; model-visible responses contain only bounded extracted text and scalar provenance/classification metadata.",
    "Content identity is the source SHA-256; derived identity includes extractor version and deterministic unit/chunk ordinal.",
    "Only current lease generations may commit chunks or unit completion; chunk insertion and unit completion are atomic.",
    "Anonymous/read-only callers cannot enqueue, retry, cancel, or mutate canvas state; trusted-service and operator scope checks are centralized and audited without token values or raw arguments.",
    "Model-facing tool execution never exposes ingestion mutations and cannot authorize an arbitrary namespaced server by matching only an allowed suffix.",
    "Missing or invalid classification remains unknown and fails closed before provider dispatch.",
    "No distributed queue or object store is introduced until measured single-host throughput, disk, or availability limits justify migration."
  ]
}
